\begin{table}[H]
    \centering
    \caption{S2 Results: External IPv6, Firewall Disabled}
    \label{tab:s2-external-ipv6-fw-disabled-results}
    \rowcolors{2}{white}{tableheader}
    \begin{tabularx}{\textwidth}{p{0.22\textwidth} p{0.20\textwidth} L}
        \toprule
        \rowcolor{uzhblue}
        \textbf{\color{white}Test} & \textbf{\color{white}Tool} & \textbf{\color{white}Result} \\
        \midrule
        ICMPv6 reachability & \texttt{ping6 -c 10} & 10/10 replies, 0\% loss, avg RTT 40.4 ms \\
        Port 80 state & \texttt{nmap -sT} & open (syn-ack) \\
        Port 23 state & \texttt{nmap -sT} & open (syn-ack) \\
        HTTP root (\texttt{/}) & \texttt{curl} & HTTP 200 OK (MAC + IPv6 exposed) \\
        HTTP hidden administrative endpoint (\texttt{/hidden\_admin}) & \texttt{curl} & HTTP 200 OK (Diagnostic Mode: ENABLED) \\
        Telnet session & \texttt{ncat} & credentials transmitted and accepted in plaintext \\
        Plaintext exposure & \texttt{tcpdump -A} & full HTTP request and response observable in plaintext \\
        \bottomrule
    \end{tabularx}
\end{table}
